# Deploy to WhoGoHost - EcoTrack-NG Production
name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, xml, ctype, iconv, mysql, zip
          coverage: none

      - name: Install PHP dependencies
        run: |
          composer install --no-ansi --no-dev --no-interaction --no-plugins --no-progress --no-scripts --optimize-autoloader

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install NPM Dependencies & Build
        run: |
          npm install --legacy-peer-deps
          npm run build
          # ⚡️ CLEANUP (Exclude heavy folders from ZIP)
          rm -rf node_modules storage/logs/*
          # 📦 ZIP the project (Excludes .git and other dev files)
          zip -r deploy.zip . -x "*.git*" "node_modules*" ".github*" "tests*"
        env:
          VITE_PUSHER_APP_KEY: ${{ secrets.VITE_PUSHER_APP_KEY }}
          VITE_PUSHER_APP_CLUSTER: ${{ secrets.VITE_PUSHER_APP_CLUSTER }}

      - name: Server Disk Check & Nuclear Cleanup
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.WHOGOHOST_SSH_HOST }}
          username: ${{ secrets.WHOGOHOST_SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.WHOGOHOST_SSH_PORT || 22 }}
          script: |
            echo "📊 Checking Disk & Inodes..."
            df -h | grep "/home/forahiao" || df -h
            df -i | grep "/home/forahiao" || df -i
            echo "🧹 Clearing target directory..."
            mkdir -p /home/forahiao/public_html/ecotrack.forahia.org.ng
            cd /home/forahiao/public_html/ecotrack.forahia.org.ng && rm -rf *

      - name: Upload Deployment Archive
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.WHOGOHOST_SSH_HOST }}
          username: ${{ secrets.WHOGOHOST_SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.WHOGOHOST_SSH_PORT || 22 }}
          source: "deploy.zip"
          target: "/home/forahiao/public_html/ecotrack.forahia.org.ng"

      - name: Extract & Post-Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.WHOGOHOST_SSH_HOST }}
          username: ${{ secrets.WHOGOHOST_SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.WHOGOHOST_SSH_PORT || 22 }}
          script: |
            cd /home/forahiao/public_html/ecotrack.forahia.org.ng
            unzip -o deploy.zip && rm deploy.zip
            php artisan migrate --force
            php artisan optimize
